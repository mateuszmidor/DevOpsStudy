# ---------------------------------------------------------------------------
# MS Teams webhook URL (kind: Secret)
# ---------------------------------------------------------------------------
# Stores the Microsoft Teams incoming webhook URL used by the
# GrafanaContactPoint CR (06-contact-point.yaml).
#
# The Grafana Operator resolves this secret via valuesFrom[].secretKeyRef
# at reconciliation time.
#
# IMPORTANT: Replace the URL below with a real Teams channel webhook URL.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: contact-point-url
  namespace: monitoring
stringData:
  # REPLACE ME: Replace with a real Teams channel webhook URL
  url: "https://webhook.site/2e5e7bcf-c8b6-46a3-9045-93f528b55e34"
